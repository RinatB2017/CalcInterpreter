package types;

import java.util.ArrayList;

public class FuncArgs {
	ArrayList<FuncPrim> args;
	
	/**
	 * Сравнивает списки аргументов, 
	 * если среди них встречает ф-ии, то вызывает метод для них.
	 * @return
	 */
	boolean equals(ArrayList<FuncPrim> sec){
		//for(;false;)
		//	if(!fp.equals(obj)) // TODO FuncArgs equals()
		return false;
	}
}
