// Перечисление названий всех возможных терминалов
public enum Names {
		NAME, NUMBER, EXIT, PLUS, MINUS, MUL, DIV,
		END, ASSIGN, LP, RP, LF, RF, ILLEGAL_TOKEN, POW,
		PRINT, ADD, DEL, RESET, SET, UNSET, HELP, STATE, IF, ELSE, SKIPABLE,
 		COS, SIN, FACTORIAL, L_COMMENT, R_COMMENT,
 		SET_NAMES, TRUE, FALSE;
}
